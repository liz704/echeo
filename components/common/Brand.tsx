"use client";
import { Icon, Card, Badge, Button, IconButton, Field, Toggle, Modal, Empty, SearchBar } from "@/components/ui";
export function Brand(){return <div className="brand"><div className="brandMark">É</div><div><b>Échéo</b><small>N’oubliez plus ce qui compte.</small></div></div>}
